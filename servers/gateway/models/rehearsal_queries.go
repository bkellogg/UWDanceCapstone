package models

// InsertNewRehearsalTimes inserts the given new rehearsal times and returns the resulting
// rehearsal times or and error if one occurred.
func (store *Database) InsertNewRehearsalTimes(nrts NewRehearsalTimes) (RehearsalTimes, *DBError) {
	return nil, nil
}
